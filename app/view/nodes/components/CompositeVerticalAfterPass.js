const CompositeVerticalPass = require('./CompositeVerticalPass');

/**
 * CompositeVerticalAfterPass - component used to composite vertical shader effects after a BaseSprite draw (i.e. renders from a texture)
 * @see CompositePass
 */
const CompositeVerticalAfterPass = CompositeVerticalPass.extend({

  // nothing yet

});

module.exports = CompositeVerticalAfterPass;
